import { Footer } from 'resonate-website'

// The site footer, exactly as every page mounts it. The copyright year comes
// from the component itself — there is nothing to pass in.
export const SiteFooter = () => <Footer />
