import { AmtaDefinition } from 'resonate-website'

// The AMTA definition band as the home page and the Music Therapy page both use
// it: a full-bleed section, dropped between other page sections. It carries its
// own heading, quotation and attribution — no props, no children.
export const DefinitionBand = () => <AmtaDefinition />
